# "결제가 실패하면 어떻게 되지?" — 질문 하나가 6개월짜리 프레임워크가 되기까지, 그리고 접기까지

2026년 3월부터 9월까지, [Baldur](https://github.com/baldurhq/baldur)라는 파이썬
프레임워크를 혼자 만들었다. 외부 API(결제·LLM·이메일)가 죽었을 때 파이썬 앱이
느려지거나 잡을 잃지 않게 하는 물건이다. 6개월, 커밋 4,977개 — 코드 2,007개, 설계·검증 문서 2,970개(왜 문서가 더 많은지는 아래에) — PyPI 릴리스 24번.
사용자는 0명이다. 왜 그렇게 됐는지, 그리고 그 과정에서 무엇이 남았는지 적는다.

## 무엇을 만들었나

한 문장으로: 외부 의존성이 죽었을 때 회로를 끊고(circuit breaker), 실패한 작업을
저장했다가(DLQ), 의존성이 돌아오면 자동으로 다시 실행하는(replay) 데코레이터 하나.

```python
@baldur.protected("summarize", dlq=True, timeout=60.0)
def summarize(doc_id: str) -> str:
    return openai_client.responses.create(...)
```

규모:

- 공개 코드 329,321줄, 1,237파일. Django·Flask·FastAPI·Celery·Gunicorn 어댑터,
  Prometheus·OpenTelemetry 연동, Redis Sentinel·PostgreSQL 백엔드
- 테스트 함수 27,519개, 아키텍처 검사 79개, 카오스·벤치마크·E2E 트리
- CI 워크플로 10개 — CodeQL, OpenSSF Scorecard, ClusterFuzzLite 퍼징, 비밀 유출 검사
- PyPI 릴리스 24번(5월 15일 0.0.0 → 9월 18일 1.15.1), 공개 레포 태그 22개, 문서 사이트

## 왜 만들었나

장고로 쇼핑몰 CRUD를 만들다가 "결제 API가 실패하면 이 주문은 어떻게 되지?"가 궁금했다.
실제로 잃은 건 없다 — 사고가 아니라 질문에서 시작했다. 답을 찾다가 서킷 브레이커를
직접 구현했고, 그게 라이브러리로 이미 있다는 건 한참 뒤에 알았다. 그때 물었어야 할
질문은 "그럼 왜 아무도 이걸 합쳐서 만들지 않았지?"였는데, 나는 "없으니까 내가 만들면
되겠다"로 읽었다. 이 한 번의 읽기가 이후 6개월을 결정했다.

## 무슨 일이 있었나

만드는 동안은 다 잘 됐다. 테스트가 통과하고, 시나리오가 통과하고, 릴리스가 나갔다.
문제는 만든 뒤에 나타났다.

**수요를 재보니** (Stack Overflow `python` 태그, 전체 기간):

| 질문 | 건수 |
|---|---|
| retry | 5,521 |
| dead letter queue | 28 |
| circuit breaker | 19 |
| "replay failed requests" | 3 |
| "when the api is down" | 0 |

사람들은 "어떻게 재시도하나"까지만 묻는다. 재시도가 소진된 뒤 잡이 어디로 가는지는
아무도 묻지 않는다. 그 통증이 커진 조직은 파이썬 라이브러리가 아니라 다른 층에서
해결한다 — SQS의 DLQ redrive, Temporal의 무제한 재시도, Envoy의 네트워크 층 회로
차단, Stripe의 3일 웹훅 재전송. 라이브러리 층이 비어 있던 건 기회가 아니라
"필요 없다"의 증거였다.

**직접 물어보니**: r/django에 제품 언급 없이 "외부 API가 한 시간 죽었을 때 백그라운드
잡은 어떻게 됐나요?"라고 올렸다. 답글 7개. 전부 자기 테이블 + 자기 스크립트 + 수동
재투입으로 처리하고 있었고, "그게 일이다"라고 했다. 라이브러리를 원하거나 이름을
댄 사람은 0명. 재투입 시 중복 실행은? — "우리 앱은 신경 안 씀".

**숫자로는**: Show HN 1점. 방문한 21명 중 star나 설치를 남긴 사람 0명. GitHub star 1개.
pypistats를 OS별로 쪼개 보니 릴리스 없는 날의 다운로드는 전부 인덱서 봇이었고,
3주 동안 사람이 pip으로 설치한 흔적은 많아야 2건이었다.

## 무엇이 틀렸나

결과가 아니라 원인을 적는다.

1. **"없다"를 "기회"로 읽었다.** 30년 된 생태계에 패키지가 60만 개다. 거기 없는 건
   대개 필요 없거나 다른 층이 흡수한 것이다. 첫 질문은 "왜 없지?"여야 했다.
2. **공급만 조사했다.** 경쟁자, 채널, 가격, 성장 사례 — 전부 "수요가 있다고 치고 어떻게
   도달하나"였다. "누가 이 질문을 하나"는 한 번도 재지 않았다. 위 표를 만드는 데
   반나절이 걸렸고, 그 데이터는 3월에도 있었다.
3. **n=1을 일반화했다.** 내 질문은 진짜였다. 틀린 건 "내가 라이브러리로 풀었으니
   남들도 라이브러리를 원한다"였다.
4. **"작동한다"를 "원한다"로 읽었다.** 테스트·시나리오·dogfood는 전부 엔지니어링 검증이다.
   시장 검증은 별개인데, 노력이 전부 앞쪽에 들어갔다.
5. **약한 신호를 매번 채널 탓으로 돌렸다.** 1점이면 제목 탓, 무반응이면 워밍업 탓,
   미회신이면 문구 탓. 다섯 번 연속이면 채널이 아니라 문제의 신호였다.

## 무엇이 남았나

제품은 실패했고, 만드는 방법은 살아남았다. 이 프로젝트는 처음부터 끝까지 AI 코딩
에이전트(Claude Code)와 함께 만들었고, 6개월 동안 배운 건 "에이전트가 빠르게 쓰는
코드를 어떻게 믿을 것인가"였다. 남은 것:

- **문서 레인.** 문제 → 초안 → 구체화 → 실행 → 검증 → 리뷰. 단계마다 전용 스킬이
  있고, 실행 문서 423개가 남았다. "무엇을 만들지"가 문서로 먼저 고정되지 않으면
  에이전트는 그럴듯한 다른 것을 만든다.
- **아키텍처 검사 79개.** 순환 import 0, 싱글턴 get/reset 쌍, "기본값 ON인 설정 플래그는
  반드시 그걸 읽는 프로덕션 코드가 있어야 한다"(주장↔배선 검사), 테스트 위치 규칙.
  에이전트가 "했다"고 말하는 것과 코드가 하는 것 사이의 차이를 기계가 잡는다.
- **반박 에이전트.** 변경의 주장 목록과 diff만 받고, 그 변경을 만든 문서와 테스트는
  읽지 못하는 읽기 전용 에이전트가 반례를 찾는다. 한 변경에서 세 번 연속 반박당한
  적이 있다 — 셋 다 맞았다.
- **시나리오 테스트베드.** Django·Flask·FastAPI·Celery 앱을 컨테이너로 띄우고 외부
  API를 실제로 죽이는 시나리오 46개. 단위 테스트가 전부 초록인데 여기서 깨진 게 여럿이다.
- **범위 밖 발견 393건의 장부.** 작업 중 발견한 문제를 그 자리에서 고치지 않고 기록해
  두었다가 한 번에 분류하는 규칙. 깊이 우선으로 따라가면 원래 작업이 끝나지 않는다.

이 규율은 이 프레임워크의 것이 아니라 에이전트와 일하는 방법의 것이라, 다음
프로젝트에 그대로 가져간다.

## 다음엔

다짐이 아니라 검사 가능한 규칙으로. 규칙마다 통과/탈락이 갈리는 기준과, Baldur가
어디서 탈락했는지를 붙였다.

1. **세고 나서 만든다.** 첫 주는 코드 0줄. 같은 질문이 공개된 곳(Stack Overflow·
   Reddit·포럼·앱 리뷰)에 최근 1년 300건 미만이면 탈락.
   — Baldur: "when the api is down" 0건.
2. **묻는 사람이 직접 못 만드는 자리만.** 상위 답변이 "나도 손으로 / 스프레드시트로
   한다"면 통과. 상위 답변이 "직접 짜세요"거나 묻는 사람이 개발자면 탈락 — AI 시대엔
   그게 한 시간짜리다.
   — Baldur: r/django 7명 전원 자작.
3. **"왜 아직 없나"의 답이 "최근에 바뀐 것"이어야 한다.** 답이 "다른 층이 흡수한다",
   "이미 있다", "돈이 안 된다"면 탈락. 답을 모르면 만드는 게 아니라 더 찾는다.
   — Baldur: 큐·워크플로 엔진·벤더가 흡수.
4. **코드를 지워도 남는 게 있어야 한다.** 데이터·연동·운영 중 하나가 가치의 본체가
   아니면 탈락. 순수 코드는 AI가 가장 잘 대체하는 물건이다.
   — Baldur: 순수 코드.
5. **만들기 전에 다섯 명.** "나오면 쓸게요"가 아니라 대기 명단·선결제·시간 약속 중
   하나를 다섯 명에게 받기 전에는 2주 이상 만들지 않는다.
   — Baldur: 0명, 6개월.
6. **0이 세 번이면 문제를 의심한다.** 제품 없는 질문 글에 "나도 그렇다"는 답이 0인
   게 세 번 연속이면 채널을 바꾸지 않고 문제를 버린다.
   — Baldur: 다섯 번 연속, 매번 채널 탓.

## 링크

- 코드: https://github.com/baldurhq/baldur (Apache 2.0, 그대로 둔다)
- 문서: https://baldur.sh
- 위 표의 원본 쿼리와 pypistats 분석은 요청하면 공유한다.
