# Architectural fitness function baselines.
#
# Each top-level key matches the `rule_key` argument used by a per-rule test
# in `tests/architecture/`. Entries are { file, symbol?, count?, reason,
# ticket, target_remove_by? } records:
#   - `file`: POSIX-style path relative to project root.
#   - `symbol` (optional): the drift-stable enclosing-scope qualname (CPython
#     `__qualname__` semantics — `<module>`, `Class.method`,
#     `outer.<locals>.inner`). When present, the match keys on (file, symbol),
#     NOT a line number, so unrelated edits above a baselined symbol no longer
#     break the gate (impl doc 534). When absent, the whole file is allowlisted
#     for this rule (infinite allowance).
#   - `count` (optional, default 1): the allowed number of violation
#     occurrences for that (file, symbol). A new occurrence pushing the live
#     count past this threshold regresses; this closes the multi-site blind
#     spot the pre-534 line keying carried.
#   - `reason`: free-text explaining why the violation is accepted.
#   - `ticket`: tracking reference (OOS row, impl doc, etc.).
#   - `target_remove_by`: ISO `YYYY-MM-DD` date this exemption is removed by.
#     A new entry MUST carry one -- the undated-entry count is an exact-match
#     monotonic ratchet (`dateless_baseline_budget` below), so an undated
#     addition fails the suite. A milestone string stays legal but counts as
#     undated: it cannot expire mechanically. Past its date, the entry fails.
#     An entry naming a deleted file fails too. Enforced by G86.
#
# Entries within each rule's list SHOULD be sorted by (file, symbol) ascending
# for stable diffs and minimal merge conflicts.
#
# Improvements (entries that no longer match a real violation) are silently
# accepted; per-rule tests print an INFO message asking for a baseline refresh.

time_handling:
  - {file: src/baldur/adapters/celery/tasks/monitoring.py, symbol: check_and_report_sla_breaches, reason: pre-existing datetime.now() - pending migration to utc_now(), ticket: '506'}
  - {file: src/baldur/adapters/django/management/commands/security_review.py, symbol: Command.handle, reason: pre-existing datetime.now() - pending migration to utc_now(), ticket: '506'}
  - {file: src/baldur/adapters/queues/rq_adapter.py, symbol: RQTaskAdapter.schedule_periodic, reason: pre-existing datetime.now() - pending migration to utc_now(), ticket: '506'}
  - {file: src/baldur/adapters/queues/sync_adapter.py, symbol: SyncTaskAdapter._execute_task, count: 5, reason: pre-existing datetime.now() - pending migration to utc_now(), ticket: '506'}
  - {file: src/baldur/services/event_bus/bus/_emergency_postmortem.py, symbol: _generate_emergency_postmortem_data, reason: pre-existing datetime.now() - pending migration to utc_now(), ticket: '506'}
  - {file: src/baldur/services/healing_events_store.py, symbol: _get_today_key, reason: pre-existing datetime.now() - pending migration to utc_now(), ticket: '506'}
  - {file: src/baldur/services/healing_events_store.py, symbol: add_healing_event_redis, reason: pre-existing datetime.now() - pending migration to utc_now(), ticket: '506'}
  - {file: src/baldur/services/healing_events_store.py, symbol: get_healing_events_count_redis, reason: pre-existing datetime.now() - pending migration to utc_now(), ticket: '506'}
  - {file: src/baldur/services/healing_events_store.py, symbol: get_healing_events_redis, reason: pre-existing datetime.now() - pending migration to utc_now(), ticket: '506'}

health_check_naming:
  - {file: src/baldur/adapters/audit/redis_buffer.py, symbol: RedisAuditBuffer.is_healthy, reason: 'pre-existing: RedisAuditBuffer.is_healthy must be @property (cached state query, no I/O)', ticket: '506'}
  - {file: src/baldur/adapters/health_checker.py, symbol: PortableHealthChecker.is_healthy, reason: 'pre-existing: PortableHealthChecker.is_healthy must be @property (cached state query, no I/O)', ticket: '506'}
  - {file: src/baldur/audit/persistence/disk_space_monitor.py, symbol: DiskSpaceMonitor.is_healthy, reason: 'pre-existing: DiskSpaceMonitor.is_healthy must be @property (cached state query, no I/O)', ticket: '506'}
  - {file: src/baldur/audit/self_audit.py, symbol: SelfAuditLogger.is_healthy, reason: 'pre-existing: SelfAuditLogger.is_healthy must be @property (cached state query, no I/O)', ticket: '506'}

startup_wiring:
  - {file: src/baldur/adapters/celery/signal_hooks.py, symbol: setup_baldur_signals, reason: 'permanent exemption: user-invoked integration API. The caller owns the Celery app object, so the library cannot invoke this - the docstring Example is the intended call site', ticket: '506'}
  - {file: src/baldur/audit/sync_worker.py, symbol: start_sync_worker, reason: 'gate reach: invoked by async_audit_lifecycle._start_sync_worker(), which is not in the entry-point set. It is now the single start path - the lifecycle delegates here rather than repeating the absorb-then-start sequence', ticket: '506'}
  - {file: src/baldur/context/celery_propagation.py, symbol: setup_celery_causation_propagation, reason: 'vestigial by design: the before_task_publish handler is connected by the @before_task_publish.connect decorator at import time. This function only records enablement state, so nothing calls it and nothing needs to', ticket: '506'}
  - {file: src/baldur/observability/baggage.py, symbol: setup_baggage_propagation, reason: 'gate reach: invoked from observability/__init__.py, which is not in the entry-point set (widening the set to cover it would make nearly every module an entry point and gut the rule)', ticket: '506'}
  - {file: src/baldur/services/cell_topology/health.py, symbol: setup_cell_health_scheduler, reason: 'gate reach: invoked from cell_topology/service.py with fault isolation, which is not in the entry-point set', ticket: '506'}

singleton_pair:
  - {file: src/baldur/adapters/memory/layered_repository/__init__.py, symbol: reset_layered_repository_executor, reason: 'pre-existing: reset_layered_repository_executor without get_layered_repository_executor', ticket: '506'}
  - {file: src/baldur/api/django/audit_middleware.py, symbol: get_audit_middleware_from_settings, reason: 'pre-existing: get_audit_middleware_from_settings without reset_audit_middleware_from_settings', ticket: '506'}
  - {file: src/baldur/api/django/rate_limit/middleware.py, symbol: reset_rate_limit_state, reason: 'pre-existing: reset_rate_limit_state without get_rate_limit_state', ticket: '506'}
  - {file: src/baldur/api/middleware/rate_limit.py, symbol: reset_rate_limit_state, reason: 'pre-existing: reset_rate_limit_state without get_rate_limit_state', ticket: '506'}
  - {file: src/baldur/audit/__init__.py, symbol: reset_loaded_symbols, reason: 'pre-existing: reset_loaded_symbols without get_loaded_symbols', ticket: '506'}
  - {file: src/baldur/audit/async_audit_lifecycle.py, symbol: reset_lifecycle_state, reason: 'pre-existing: reset_lifecycle_state without get_lifecycle_state', ticket: '506'}
  - {file: src/baldur/audit/cascade_auditor/_wal_recovery.py, symbol: reset_wal_writer, reason: 'pre-existing: reset_wal_writer without get_wal_writer', ticket: '506'}
  - {file: src/baldur/bootstrap.py, symbol: reset_init_state, reason: 'pre-existing: reset_init_state without get_init_state', ticket: '506'}
  - {file: src/baldur/services/circuit_breaker/config.py, symbol: reset_circuit_breaker_config, reason: 'holder read accessor is current_circuit_breaker_config() - named for the currently-installed shared value, not a lazily-constructed singleton getter', ticket: '744'}
  - {file: src/baldur/coordination/factory.py, symbol: reset_leader_electors, reason: 'pre-existing: reset_leader_electors without get_leader_electors', ticket: '506'}
  - {file: src/baldur/coordination/scheduler.py, symbol: reset_schedulers, reason: 'pre-existing: reset_schedulers without get_schedulers', ticket: '506'}
  - {file: src/baldur/coordination/shutdown_integration.py, symbol: reset_registered_electors, reason: 'pre-existing: reset_registered_electors without get_registered_electors', ticket: '506'}
  - {file: src/baldur/metrics/event_handlers.py, symbol: reset_event_handler_cache, reason: 'pre-existing: reset_event_handler_cache without get_event_handler_cache', ticket: '506'}
  - {file: src/baldur/metrics/recorders/circuit_breaker.py, symbol: reset_blocked_recorder, reason: 'pre-existing: reset_blocked_recorder without get_blocked_recorder', ticket: '506'}
  - {file: src/baldur/observability/__init__.py, symbol: reset_opentelemetry, reason: 'pre-existing: reset_opentelemetry without get_opentelemetry', ticket: '506'}
  - {file: src/baldur/observability/log_processors.py, symbol: reset_rate_limit_state, reason: 'pre-existing: reset_rate_limit_state without get_rate_limit_state', ticket: '506'}
  - {file: src/baldur/observability/log_processors.py, symbol: reset_strict_validation_cache, reason: 'pre-existing: reset_strict_validation_cache without get_strict_validation_cache', ticket: '506'}
  - {file: src/baldur/observability/structlog_config.py, symbol: reset_structlog_config, reason: 'pre-existing: reset_structlog_config without get_structlog_config', ticket: '506'}
  - {file: src/baldur/protect_facade.py, symbol: reset_protect_caches, reason: 'pre-existing: reset_protect_caches without get_protect_caches', ticket: '506'}
  - {file: src/baldur/services/bulkhead/metrics.py, symbol: reset_bulkhead_metrics, reason: 'pre-existing (relocated): reset_bulkhead_metrics without get_bulkhead_metrics', ticket: '709'}
  - {file: src/baldur/services/dlq_capture/overflow.py, symbol: reset_overflow_state, reason: 'pre-existing (relocated): reset_overflow_state without get_overflow_state', ticket: '707'}
  - {file: src/baldur/services/dlq_outbox/outbox.py, symbol: reset_dlq_outbox, reason: 'pre-existing: reset_dlq_outbox without get_dlq_outbox', ticket: '506'}
  - {file: src/baldur/services/precomputed_cache/multi_tier.py, symbol: reset_drift_stats, reason: 'pre-existing: reset_drift_stats without get_drift_stats', ticket: '506'}
  - {file: src/baldur/settings/layered_provider.py, symbol: get_layered_settings, reason: 'pre-existing: get_layered_settings without reset_layered_settings', ticket: '506'}
  - {file: src/baldur/tasks/base.py, symbol: reset_cooldowns, reason: 'pre-existing: reset_cooldowns without get_cooldowns', ticket: '506'}

all_declaration: []
state_backend_ttl:
  - {file: src/baldur/adapters/redis/dlq_compression.py, symbol: RedisDLQCompression._record_compression_audit, reason: 'pre-existing: receiver heuristic FP - not actually a StateBackend (e.g., metric gauge)', ticket: '506'}
  - {file: src/baldur/metrics/otel_backend.py, symbol: _GaugeStoreShimChild.set, reason: 'receiver heuristic FP - _store is a _GaugeStore metric value store, not a StateBackend', ticket: '646'}
  - {file: src/baldur/metrics/otel_backend.py, symbol: _OTELCBRecorder.set_state, reason: 'pre-existing: receiver heuristic FP - not actually a StateBackend (e.g., metric gauge)', ticket: '506'}
  - {file: src/baldur/metrics/otel_backend.py, symbol: _OTELDLQRecorder.set_pending_count, reason: 'pre-existing: receiver heuristic FP - not actually a StateBackend (e.g., metric gauge)', ticket: '506'}
  - {file: src/baldur/metrics/otel_backend.py, symbol: _OTELDLQRecorder.set_size_ratio, reason: 'pre-existing: receiver heuristic FP - not actually a StateBackend (e.g., metric gauge)', ticket: '506'}
  - {file: src/baldur/metrics/otel_backend.py, symbol: _OTELDLQRecorder.set_status_count, reason: 'pre-existing: receiver heuristic FP - not actually a StateBackend (e.g., metric gauge)', ticket: '506'}
  - {file: src/baldur/metrics/otel_backend.py, symbol: _OTELInfraRecorder.set_active_connections, reason: 'pre-existing: receiver heuristic FP - not actually a StateBackend (e.g., metric gauge)', ticket: '506'}
  - {file: src/baldur/metrics/otel_backend.py, symbol: _OTELInfraRecorder.set_capacity_active_events, reason: 'pre-existing: receiver heuristic FP - not actually a StateBackend (e.g., metric gauge)', ticket: '506'}
  - {file: src/baldur/metrics/otel_backend.py, symbol: _OTELInfraRecorder.set_capacity_pool_multiplier, reason: 'pre-existing: receiver heuristic FP - not actually a StateBackend (e.g., metric gauge)', ticket: '506'}
  - {file: src/baldur/metrics/otel_backend.py, symbol: _OTELInfraRecorder.set_capacity_rate_multiplier, reason: 'pre-existing: receiver heuristic FP - not actually a StateBackend (e.g., metric gauge)', ticket: '506'}
  - {file: src/baldur/metrics/otel_backend.py, symbol: _OTELInfraRecorder.set_error_rate, reason: 'pre-existing: receiver heuristic FP - not actually a StateBackend (e.g., metric gauge)', ticket: '506'}
  - {file: src/baldur/metrics/otel_backend.py, symbol: _OTELInfraRecorder.set_latency_percentile, reason: 'pre-existing: receiver heuristic FP - not actually a StateBackend (e.g., metric gauge)', ticket: '506'}
  - {file: src/baldur/metrics/otel_backend.py, symbol: _OTELInfraRecorder.set_mesh_overrides_active, reason: 'pre-existing: receiver heuristic FP - not actually a StateBackend (e.g., metric gauge)', ticket: '506'}
  - {file: src/baldur/metrics/otel_backend.py, symbol: _OTELInfraRecorder.set_request_queue_depth, reason: 'pre-existing: receiver heuristic FP - not actually a StateBackend (e.g., metric gauge)', ticket: '506'}
  - {file: src/baldur/metrics/otel_backend.py, symbol: _OTELInfraRecorder.set_worker_utilization, reason: 'pre-existing: receiver heuristic FP - not actually a StateBackend (e.g., metric gauge)', ticket: '506'}
  - {file: src/baldur/metrics/otel_backend.py, symbol: _OTELRetryRecorder.set_success_rate, reason: 'pre-existing: receiver heuristic FP - not actually a StateBackend (e.g., metric gauge)', ticket: '506'}
  - {file: src/baldur/metrics/recorders/watchdog.py, symbol: WatchdogMetricRecorder.set_self_cb_state, reason: 'pre-existing: receiver heuristic FP - not actually a StateBackend (e.g., metric gauge)', ticket: '506'}
  - {file: src/baldur/services/capacity_reservation/event_calendar.py, symbol: EventCalendar._persist, reason: 'pre-existing: receiver heuristic FP - not actually a StateBackend (e.g., metric gauge)', ticket: '506'}
  - {file: src/baldur/services/pending_config.py, symbol: PendingConfigService.create_pending_change, reason: '666 per-key pending storage: transient change keys with explicit lifecycle (apply/cancel delete + cleanup_expired); no fixed TTL because the scheduled delay is operator-variable', ticket: '666'}
  - {file: src/baldur/services/pending_config.py, symbol: PendingConfigService._migrate_legacy_blob, reason: '666 one-time legacy-blob migration: hydrates per-key change entries (same lifecycle as create_pending_change); no fixed TTL', ticket: '666'}
  - {file: src/baldur/services/system_control.py, symbol: SystemControlManager._save_state, reason: 'pre-existing: receiver heuristic FP - not actually a StateBackend (e.g., metric gauge)', ticket: '506'}

env_factory_bodies:
  - {file: "src/baldur/adapters/airgap/factory.py", reason: "pre-existing: OOS #453 named offender - pending BaldurRuntime migration", ticket: "506"}
  - {file: "src/baldur/adapters/audit/singleton.py", reason: "pre-existing: OOS #453 named offender - pending BaldurRuntime migration", ticket: "506"}
  - {file: "src/baldur/adapters/metrics/factory.py", reason: "pre-existing: OOS #453 named offender - pending BaldurRuntime migration", ticket: "506"}
  - {file: "src/baldur/audit/checkpoint/__init__.py", reason: "pre-existing: OOS #453 named offender - pending BaldurRuntime migration", ticket: "506"}

optional_imports:
  - {file: "src/baldur/adapters/airgap/factory.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/cache/memcached_adapter.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/celery/baldur_task.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/celery/beat_schedule.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/celery/signal_handlers.py", reason: "511 D13: no-op setup_logging handler — celery.signals import gated by celery extras", ticket: "511"}
  - {file: "src/baldur/adapters/celery/bootstrap_hooks.py", reason: "777 D1: worker-lifecycle receivers — celery.signals import gated by celery extras; every reach-through requires celery already imported", ticket: "777"}
  - {file: "src/baldur/adapters/celery/signal_hooks.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/celery/tasks/cell_evacuation.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/celery/tasks/config_shadow.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/celery/tasks/dlq_replay.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/celery/tasks/monitoring.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/celery/tasks/persistence.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/celery/tasks/postmortem.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/celery/tasks/sla_notification.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/database/django_health.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/deployment/__init__.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/django/admin.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/django/apps.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/django/auto_config.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/django/management/commands/baldur_config.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/django/management/commands/check_baldur_config.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/django/management/commands/security_review.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/django/migrations/0001_initial.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/django/migrations/0002_add_dlq_and_security_models.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/django/migrations/0003_delete_cascade_event_archive.py", reason: "django migration module shape - django import is unconditional by construction", ticket: "713"}
  - {file: "src/baldur/adapters/django/models/_abstract_audit_log.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/django/models/_abstract_failed_external_request.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/django/repositories/postmortem.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/django/repositories/recovery_session.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/django/security_incident.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/django/session_adapter.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/django/signal_hooks.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/django/startup/env_auditor.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/django/startup/rbac_initializer.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/django/statistics.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/flask/middleware.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/metrics/django_adapter.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/postgres/sessions.py", reason: "515 D4: function-body django import only reached via registry-gated 'django' provider (factory/adapters.py discover_pg_admin_adapters)", ticket: "515"}
  - {file: "src/baldur/adapters/prometheus_adapter.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/queues/arq_adapter.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/queues/celery_adapter.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/queues/rq_adapter.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/adapters/redis/connection_factory.py", reason: "513 D1: redis moved from core deps to [redis] extra — imports are function-local inside RedisConnectionFactory methods, only reached when redis adapter is explicitly selected", ticket: "513"}
  - {file: "src/baldur/adapters/sqlalchemy_pool.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/adapter.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/admission_control.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/audit_middleware.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/base.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/cell/middleware.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/exceptions/handler.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/middleware/actor_context.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/middleware/backpressure.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/middleware/baldur.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/middleware/drain_aware.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/middleware/health_bridge.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/middleware/ip_ban.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/permissions.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/pool_circuit_breaker.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/rate_limit/middleware.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/rate_limit/redis_health_checker.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/reauthentication.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/serializers/chaos.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/serializers/config/advanced_configs.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/serializers/config/base.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/serializers/config/core_configs.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/serializers/config/pending_configs.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/serializers/config/security_configs.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/serializers/config/slo_configs.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/serializers/config/storage_configs.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/serializers/control.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/serializers/features.py", reason: "django subtree assumes DRF; same accepted state as sibling serializers", ticket: "530"}
  - {file: "src/baldur/api/django/serializers/metric_sync.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/stress_views.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/throttle_adapter.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/tiering/middleware.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/scaling/tiering/circuit_breaker.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/audit.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/auto_tuning.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/canary.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/cascade.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/chaos.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/compliance.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/config_history.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/control.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/dlq.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/dna.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/emergency.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/error_budget.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/features.py", reason: "django subtree assumes django.urls; same accepted state as sibling url modules", ticket: "530"}
  - {file: "src/baldur/api/django/urls/governance.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/grafana_webhook.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/health.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/l2_storage.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/meta_watchdog.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/postmortem.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/recovery.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/reports.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/runtime_config.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/schema.py", reason: "django subtree assumes django.urls; drf-spectacular import is already conditional", ticket: "530"}
  - {file: "src/baldur/api/django/urls/stress.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/system_control.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/tiering.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/urls/xtest.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/views/bulkhead.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/views/xtest/base.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/views/xtest/circuit_breaker.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/views/xtest/dlq.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/views/xtest/error_budget.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/views/xtest/idempotency.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/views/xtest/integration.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/views/xtest/observability.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/views/xtest/rate_limit.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/views/xtest/replay.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/views/xtest/retry.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/views/xtest/scenarios/base.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/views/xtest/scenarios/regional.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/views/xtest/snapshot.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/api/django/views/xtest/throttle_simulation.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/audit/redis_batch_lua.py", reason: "513 D1: redis moved from core deps to [redis] extra — module itself is lazy-imported only via adapters/audit/redis_buffer.py method-local import, never on default OSS paths", ticket: "513"}
  - {file: "src/baldur/bootstrap.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/bridges/tenacity/policy.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/celery_tasks/audit_flush_tasks.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/celery_tasks/chaos_tasks.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/celery_tasks/circuit_breaker_tasks.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/celery_tasks/dlq_tasks.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/celery_tasks/drift_detection_tasks.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/celery_tasks/metrics_tasks.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/context/celery_cell_propagation.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/context/celery_propagation.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/meta/recovery_adapter.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/metrics/endpoint_normalizer.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/metrics/recorders/daemon_worker.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/services/replay_service/arming.py", reason: "781: broker connect-timeout read - celery import gated by the celery_missing link, which returns before any broker work when the extra is absent", ticket: "781", target_remove_by: 2026-12-01}
  - {file: "src/baldur/settings/system_control.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/tasks/canary_watchdog.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/tasks/chaos_scheduler.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/tasks/cleanup_tasks.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/tasks/compliance_tasks.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/tasks/config_apply.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/tasks/intelligence_tasks.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}
  - {file: "src/baldur/tasks/traffic_aware_replay.py", reason: "pre-existing unguarded import - pending guard or extras isolation", ticket: "506"}

no_print:
  - {file: src/baldur/adapters/alert/stdout_adapter.py, symbol: StdoutAlertAdapter.resolve, count: 2, reason: pre-existing print() - pending migration to structlog, ticket: '506'}
  - {file: src/baldur/adapters/alert/stdout_adapter.py, symbol: StdoutAlertAdapter.send, reason: pre-existing print() - pending migration to structlog, ticket: '506'}
  - {file: src/baldur/adapters/audit/stdout_adapter.py, symbol: StdoutAuditLogAdapter.log, reason: pre-existing print() - pending migration to structlog, ticket: '506'}
  - {file: src/baldur/audit/continuous_audit.py, symbol: ContinuousAuditRecorder._record_with_integrity, reason: pre-existing print() - pending migration to structlog, ticket: '506'}
  - {file: src/baldur/audit/export.py, symbol: main, count: 6, reason: pre-existing print() - pending migration to structlog, ticket: '506'}
  - {file: src/baldur/audit/resilience/syslog_fallback.py, symbol: SyslogFallback._write_to_syslog, reason: pre-existing print() - pending migration to structlog, ticket: '506'}
  - {file: src/baldur/audit/resilient_recorder.py, symbol: ResilientContinuousAuditRecorder._write_to_stderr, reason: pre-existing print() - pending migration to structlog, ticket: '506'}
  - {file: src/baldur/audit/self_audit.py, symbol: SelfAuditLogger.log, reason: pre-existing print() - pending migration to structlog, ticket: '506'}
  - {file: src/baldur/audit/signed_manifest.py, symbol: main, count: 4, reason: pre-existing print() - pending migration to structlog, ticket: '506'}
  - {file: src/baldur/audit/verify_audit_integrity.py, symbol: main, count: 4, reason: pre-existing print() - pending migration to structlog, ticket: '506'}
  - {file: src/baldur/services/dlq_capture/service.py, symbol: DLQCaptureService._write_to_local_fallback, reason: pre-existing (relocated) print() - stderr last resort, pending migration to structlog, ticket: '707'}

env_prefix_naming:
  # BaldurSettings (root.py) is the root configuration object — uses empty
  # env_prefix so root-level env vars like BALDUR_CLUSTER_ID can be read
  # without a per-domain prefix. Permanent waiver.
  - {file: "src/baldur/settings/root.py", reason: "508 D9 permanent waiver: BaldurSettings is the root settings, empty env_prefix is intentional", ticket: "508"}
  # OpenTelemetrySettings follows the OpenTelemetry SDK env var standard
  # (OTEL_* via validation_alias on each field), not BALDUR_*. Empty
  # env_prefix is intentional. Permanent waiver.
  - {file: "src/baldur/settings/otel.py", reason: "508 D9 permanent waiver: OpenTelemetry SDK standard env vars (OTEL_*) via validation_alias, not BALDUR_*", ticket: "508"}
  # 508 S4 (D9 batch 2) closed — all 28 module/prefix divergences renamed +
  # kafka.py -> kafka_audit.py file rename completed. Baseline empty for D9.

package_status_marker:
  # 508 S6 batch 3 closed — all sub-packages now carry Status: markers.
# Generated: 35 module-level sites, 358 total imports (post-518-a)

pro_import_boundary:
  # 519 PR 3 (d) types track: 23 module-level rows flipped to OSS canonical
  # (baldur.models.*, baldur.interfaces.*) or PEP 562 lazy (BulkheadPolicy /
  # ThrottlePolicy via resilience/policies/__init__.py __getattr__). The 4
  # hedging rows below stay PRO-side per PR 3 deviation — see Implementation
  # Deviations from Doc (519 OSS/PRO callsite migration post-GA)
  # ("Hedging C-d1 rule 1 misapplied").
  - {file: src/baldur/resilience/policies/hedging.py, symbol: <module>, count: 5, reason: '519 PR 3 deviation + 526 P6 sub-PR 3: 5 module-level baldur_pro hedging imports (1 TYPE_CHECKING-only HedgingResult narrowing used in cast() + 4 PR-3 runtime-type deviations where C-d1 rule 1 ''TYPE_CHECKING-only marker'' does not match the actual call sites). See Implementation Deviations from Doc (519)', ticket: 519-d-deviation, category: types, target_remove_by: post-v1.1}
  # 519 PR 3 removed:
  #   - adapters/memory/layered_repository/base.py:29 — Bulkhead → baldur.interfaces.bulkhead Protocol
  #   - api/django/throttle_adapter.py:24 — ThrottleConfig → baldur.settings.throttle.ThrottleSettings alias
  #   - core/__init__.py:100 — pool_monitor 6 types → baldur.interfaces.pool_monitor (5 existing + ConnectionPoolMonitor new Protocol)
  #   - core/pool_watchdog.py:23 — same set of pool_monitor types
  #   - interfaces/statistics.py:75 — CleanupStats → baldur.models.dlq (D-d2 relocate)
  #   - meta/shutdown.py:18 — SelfHealerWatchdog → baldur.interfaces.meta_watchdog.SelfhealerWatchdog
  #   - resilience/policies/__init__.py:84 — BulkheadPolicy → PEP 562 __getattr__ (C-d1 rule 4)
  #   - resilience/policies/__init__.py:95 — ThrottlePolicy TYPE_CHECKING removed (PEP 562 lazy already present)
  #   - services/circuit_breaker/adaptive_threshold.py:35 — EmergencyModeManager TYPE_CHECKING → baldur.interfaces.emergency.EmergencyManager
  #   - services/circuit_breaker/panic_threshold.py:40 — same
  #   - services/config_shadow/evaluators/live_canary.py:21 — PassCriteria → baldur.models.canary (D-d2 relocate)
  #   - services/correlation_engine/emergency_decision.py:44 — EmergencyLevel → baldur.models.emergency (D-d1 already OSS)
  #   - services/regional_emergency/__init__.py:69 — ScopedEmergencyState → baldur.models.emergency (D-d2 relocate)
  #   - services/regional_emergency/cascade_detector.py:39 — EmergencyScope → baldur.models.emergency (D-d2 relocate)
  #   - services/regional_emergency/health_penalty.py:37 — EmergencyScope → same
  #   - services/regional_emergency/tracker.py:40 — EmergencyScope → same
  #   - services/regional_emergency/tracker.py:44 — ScopedEmergencyState → same
  #   - services/runbook/approval_gate.py:49 — UnifiedNotificationManager → baldur.interfaces.notification (new Protocol)
  #   - services/runbook/execution_models.py:23 — BlockReason/GovernanceCheckResult → baldur.models.governance (D-d1 already OSS)
  #   - services/runbook/executor.py:63 — DistributedRecoveryLock → baldur.interfaces.runbook (new Protocol)
  #   - services/runbook/executor.py:69 — IdempotencyRecord → baldur.interfaces.runbook (new Protocol)
  #   - services/settings_recommendation/service.py:47 — CanaryRolloutService → baldur.interfaces.canary (existing Protocol)
  #   - tasks/canary_watchdog.py:45 — CanaryRollout → baldur.interfaces.canary (new Protocol)

# G17b global count snapshot (module-level + in-function combined)
# 519 G1+G2: -2 statements (StepResult relocated to baldur.models.saga, primitives.py + runbook_registry.py flipped)
# 519 PR 2 (c) track: 5 module-level + ~120 in-function statements migrated to ProviderRegistry.<slot>.safe_get() — count dropped 338 -> 213
# 519 PR 3 (d) types track: 23 module-level + ~30 in-function statements migrated to baldur.models.* / baldur.interfaces.* / PEP 562 lazy — count dropped 213 -> 161. 4 hedging rows retained per deviation (see Implementation Deviations from Doc).
# Re-snapshot: 161 was carried unchanged through work that kept retiring callsites, so the
# ratchet had accumulated 27 statements of slack — it only fails on an increase, so a
# decrease never forces an update. Measured 141 at the public-repo cutover and 134 now
# (protect_facade, resilience/policies/__init__ + presets, cell_topology/health + registry,
# tasks/integrity_tasks, audit/wal/__init__). Snapshot lowered to the measured count.
pro_import_boundary_count: 134

# G17c — OSS->Dormant import-boundary global-count snapshot (528 D10-v2).
# Target = 0. Current sites (in-function lazy imports of baldur_dormant
# adapters): adapters/gunicorn/hooks.py:_reset_kafka_after_fork,
# services/event_bus/redis_bus.py:
# _publish_to_kafka_fallback, services/rate_limit/distributed_channel.py:
# _ensure_kafka_bus, scripts/demo_zero_data_loss.py. These will retire as
# OSS callers migrate fully to ProviderRegistry.kafka_eventbus /
# audit_kafka_adapter resolution.
# 599 D10 batch C — +2: beat_schedule private-lane imports for the relocated
# compliance check + learning insight task modules (saga/finops lane pattern).
dormant_import_boundary_count: 7

# G86 ratchet -- entries carrying no machine-checkable `target_remove_by`.
# Monotonic-DECREASING, exact match: a new undated entry pushes the count above
# this budget and fails; dating or removing one pushes it below and fails until
# the budget is lowered in the same change, so the slack can never be silently
# reclaimed. Give a new entry an ISO `YYYY-MM-DD` target_remove_by instead of
# raising this number. Per-repo value: the two baselines are independently
# scrubbed copies with no auto-sync (G41/G67 precedent).
dateless_baseline_budget: 259

# G19 — tests/ may not module-level-import baldur_pro / baldur_dormant
# without a `pytest.importorskip("baldur_pro")` (or "baldur_dormant") call
# or a `pytestmark = pytest.mark.requires_pro` declaration at module top.
# Per impl doc 528 Stage 5, expected violation count is zero after Stage 4
# Step 1's bulk-edit (`scripts/add_requires_pro_marker.py`) lands. Survivors
# would carry `ticket: 528-followup` per the rule registry note.
oss_test_pro_marker: []

# G20 - no tests/ file may import a baldur_pro / baldur_dormant PRIVATE
# symbol (`_foo`, excluding `__all__`/dunders), a private-module path
# (`...adaptive._helpers...`), or a wildcard (`import *`) from a gated
# top-level (impl doc 533 D5/D6). ENFORCED-EMPTY: a baseline entry would
# whitelist a private import in a file that *stays* under tests/ - but
# tests/ ships wholesale at 7.5G-1, so the entry would publish the exact
# PRO-internal leak 533 exists to prevent. A genuinely un-splittable
# private-importer is MOVED to tests/pro/ (or rewritten to the public API),
# never baselined (533 D7). The G20 test meta-asserts this key stays empty.
oss_test_pro_private_import: []

# G21 - no tests/ TEST file may classify as pure_pro / support_only per
# the import-graph classifier (scripts/classify_pro_importing_tests.py, the
# single source of truth shared with this gate - impl doc 533 D12). Such a
# file's SUT is PRO and it ships at the next public push (G2 recurs).
# ENFORCED-EMPTY for the same reason as G20: a baseline entry would whitelist
# the leak it documents. A new pure_pro/support_only test is MOVED to
# tests/pro/, never baselined. The G21 test meta-asserts this key stays empty.
oss_test_pro_classification: []

# G22 - `_current_domain.set(...)` may only be called from the canonical owner
# `src/baldur/decorators/domain_tag.py` (impl doc 545 D5 chokepoint 5). Every
# other caller MUST go through `set_domain_context()` so the OTel-baggage /
# Celery legacy-header restore path inherits the validation + fallback that
# 545 D1 added. A bypass entry here would mean a known regression that
# reintroduces the unbounded-domain bug 545 closes — fix the call site
# instead.
domain_set_direct_call: []

# G79 - every `.labels(domain=...)` write must route its value through
# `resolve_domain_label` so the cardinality cap is enforced rather than
# advisory (impl doc 738 D11). Entries below are channels whose domains no
# declaration site registers yet: routing them today would collapse every one
# of their series to the fallback label, which is a regression, not a fix.
domain_label_resolution_funnel:
  - {file: src/baldur/adapters/audit/redis_buffer.py, symbol: RedisAuditBuffer._check_buffer_threshold, count: 2, reason: 'audit-buffer channel has no registration site - funnelling it today would collapse every audit_buffer_* series to the fallback label', ticket: '738'}
  - {file: src/baldur/adapters/audit/redis_buffer.py, symbol: RedisAuditBuffer.apply_safety_ltrim, reason: 'audit-buffer channel has no registration site - funnelling it today would collapse every audit_buffer_* series to the fallback label', ticket: '738'}
  - {file: src/baldur/metrics/audit_buffer_metrics.py, symbol: record_batch_write, count: 2, reason: 'audit-buffer channel has no registration site - funnelling it today would collapse every audit_buffer_* series to the fallback label', ticket: '738'}
  - {file: src/baldur/metrics/audit_buffer_metrics.py, symbol: record_flush, reason: 'audit-buffer channel has no registration site - funnelling it today would collapse every audit_buffer_* series to the fallback label', ticket: '738'}
  - {file: src/baldur/metrics/audit_buffer_metrics.py, symbol: record_orphan_recovery, reason: 'audit-buffer channel has no registration site - funnelling it today would collapse every audit_buffer_* series to the fallback label', ticket: '738'}
  - {file: src/baldur/metrics/audit_buffer_metrics.py, symbol: record_safety_ltrim, count: 2, reason: 'audit-buffer channel has no registration site - funnelling it today would collapse every audit_buffer_* series to the fallback label', ticket: '738'}
  - {file: src/baldur/metrics/audit_buffer_metrics.py, symbol: update_buffer_metrics, count: 2, reason: 'audit-buffer channel has no registration site - funnelling it today would collapse every audit_buffer_* series to the fallback label', ticket: '738'}
  - {file: src/baldur_pro/services/error_budget/propagation.py, symbol: DomainPropagationMultiplier._record_cycle_detected_metric, reason: 'error-budget crisis-propagation channel has no registration site - same class as the audit-buffer channel, found by the 738 gate sweep', ticket: '738'}

# G30 - every operator-tunable BALDUR_* var on a gated surface
# (docs/reference/env-vars.md + docs/concepts/**/*.md) MUST resolve to a real
# (env_prefix, field) via longest-prefix match, and every concept-guide
# Configuration "Default" cell MUST equal the field default (impl doc 568).
# ENFORCED-EMPTY: a phantom var is a silent operator no-op — it is FIXED
# (renamed to the real BALDUR_<PREFIX>_<FIELD> or removed), never baselined.
env_vars_allowlist_resolves: []

# G31 - a strong-guarantee docstring keyword (impl doc 575 D1: exactly-once /
# at-least-once / at-most-once / zero[- ]data[- ]loss / linearizable / wait-free,
# matched case-insensitively) MUST carry an adjacent `# verified-by: <test_ref>`
# comment whose test resolves to a real `def` under tests/ (D2/D2-exists).
# NEAR-EMPTY (D6): each pre-existing claim is triaged into link / soften /
# baseline-genuine-test-debt; outcome (3) — claim is true but a rigorous proving
# test is real work — is the ONLY path to an entry. "Haven't looked yet" is not.
assurance_claim: []

# G32 - a long-form `*_enabled` / `enable_*` settings flag whose every consumer
# read is an echo-into-a-report-dict, or which has no read at all, is DEAD: an
# advertised guarantee no production path gates on (impl doc 575 D4,
# claim-wiring bug class). The 13 flags G32 originally surfaced beyond #574's
# already-removed set were burned down by impl doc 580 (12 removed outright; the
# echo-only scaling.rate_limit_enabled removed + its SOC2 attestation truthed).
# ENFORCED-EMPTY: a NEW dead flag is fixed by removing it (or wiring a real
# behavioral consumer) at source, never baselined.
flag_consumer_reachability: []

# G72 (722) — a boolean config field with ZERO production read is a dead flag:
# the operator sets it, nothing consumes it, and the claim it advertises is
# silently absent. Population = every `x: bool = <bool>` class-body declaration
# under src/baldur (bare `enabled` and observed-state DTO classes excluded);
# reads are counted across every installed tier.
# SEEDED, RATCHETED BOTH DIRECTIONS: this rule's test fails on a stale entry too
# — a baselined field that gets wired or deleted MUST leave this list in the same
# commit. Whole-file entries (no `symbol:`) are rejected for this rule key.
# Per-field wire-vs-delete disposition is owned by the field's own surface, not
# by this baseline; the baseline only tracks.
dead_boolean_config_fields:
  - {file: src/baldur/core/tls.py, symbol: TLSConfig.verify_ssl, reason: dead flag - zero production read (probe-verified 2026-07-20), ticket: '722'}
  - {file: src/baldur/models/recovery.py, symbol: RecoveryGateConfig.gradual_recovery, reason: serialized-live - round-trips through the emergency config API via to_dict/from_dict, no behavioral read (verified 2026-07-22), ticket: '722'}
  - {file: src/baldur/services/circuit_breaker/models.py, symbol: CircuitBreakerAdvancedConfig.allow_manual_override_in_lockdown, reason: dead flag - zero production read (probe-verified 2026-07-20), ticket: '722'}
  - {file: src/baldur/services/circuit_breaker/models.py, symbol: CircuitBreakerAdvancedConfig.blast_radius_block_on_critical, reason: dead flag - zero production read (probe-verified 2026-07-20), ticket: '722'}
  - {file: src/baldur/services/circuit_breaker/models.py, symbol: CircuitBreakerAdvancedConfig.blast_radius_integration, reason: dead flag - zero production read (probe-verified 2026-07-20), ticket: '722'}
  - {file: src/baldur/services/circuit_breaker/models.py, symbol: CircuitBreakerAdvancedConfig.freeze_on_lockdown, reason: dead flag - zero production read (probe-verified 2026-07-20), ticket: '722'}
  - {file: src/baldur/services/circuit_breaker/models.py, symbol: OpenStrategy.force_after_timeout, reason: dead flag - zero production read (probe-verified 2026-07-20), ticket: '722'}
  - {file: src/baldur/settings/arq_task.py, symbol: ArqTaskSettings.redis_ssl, reason: dead flag - zero production read; the queue adapter builds its connection without it (verified 2026-07-22), ticket: '722'}
  - {file: src/baldur/settings/bridge.py, symbol: BridgeSettings.tenacity_metrics, reason: dead flag - zero production read (probe-verified 2026-07-20), ticket: '722'}
  - {file: src/baldur/settings/chaos.py, symbol: ChaosSettings.dry_run_default, reason: dead flag - zero production read (probe-verified 2026-07-20), ticket: '722'}
  - {file: src/baldur/settings/chaos.py, symbol: ChaosSettings.require_approval, reason: dead flag - zero production read (probe-verified 2026-07-20), ticket: '722'}
  - {file: src/baldur/settings/circuit_breaker_advanced.py, symbol: CircuitBreakerAdvancedSettings.allow_manual_override_in_lockdown, reason: dead flag - zero production read (probe-verified 2026-07-20), ticket: '722'}
  - {file: src/baldur/settings/circuit_breaker_advanced.py, symbol: CircuitBreakerAdvancedSettings.blast_radius_block_on_critical, reason: dead flag - zero production read (probe-verified 2026-07-20), ticket: '722'}
  - {file: src/baldur/settings/circuit_breaker_advanced.py, symbol: CircuitBreakerAdvancedSettings.blast_radius_integration, reason: dead flag - zero production read (probe-verified 2026-07-20), ticket: '722'}
  - {file: src/baldur/settings/circuit_breaker_advanced.py, symbol: CircuitBreakerAdvancedSettings.freeze_on_lockdown, reason: dead flag - zero production read (probe-verified 2026-07-20), ticket: '722'}
  - {file: src/baldur/settings/forensic.py, symbol: ForensicSettings.collect_request_body, reason: dead flag - zero production read (probe-verified 2026-07-20), ticket: '722'}
  - {file: src/baldur/settings/forensic.py, symbol: ForensicSettings.collect_response_body, reason: dead flag - zero production read (probe-verified 2026-07-20), ticket: '722'}
  - {file: src/baldur/settings/forensic.py, symbol: ForensicSettings.include_local_variables, reason: dead control surface - no behavioral read; value only echoed via wholesale settings serialization (verified 2026-07-22), ticket: '722'}
  - {file: src/baldur/settings/forensic.py, symbol: ForensicSettings.mask_internal_ip, reason: dead flag - zero production read (probe-verified 2026-07-20), ticket: '722'}
  - {file: src/baldur/settings/forensic.py, symbol: ForensicSettings.mask_sensitive_fields, reason: dead control surface - no behavioral read; masking runs unconditionally off the pattern list (verified 2026-07-22), ticket: '722'}
  - {file: src/baldur/settings/forensic.py, symbol: ForensicSettings.mask_server_paths, reason: dead flag - zero production read (probe-verified 2026-07-20), ticket: '722'}
  - {file: src/baldur/settings/forensic.py, symbol: ForensicSettings.sanitize_sensitive_data, reason: dead control surface - no behavioral read; value only echoed via wholesale settings serialization (verified 2026-07-22), ticket: '722'}
  - {file: src/baldur/settings/logging_settings.py, symbol: LoggingSettings.include_request_id, reason: dead control surface - no behavioral read; value only echoed via wholesale settings serialization (verified 2026-07-22), ticket: '722'}
  - {file: src/baldur/settings/logging_settings.py, symbol: LoggingSettings.include_timestamps, reason: dead control surface - no behavioral read; value only echoed via wholesale settings serialization (verified 2026-07-22), ticket: '722'}
  - {file: src/baldur/settings/logging_settings.py, symbol: LoggingSettings.include_user_info, reason: dead control surface - no behavioral read; value only echoed via wholesale settings serialization (verified 2026-07-22), ticket: '722'}
  - {file: src/baldur/settings/root.py, symbol: BaldurSettings.debug_mode, reason: dead flag - zero production read (probe-verified 2026-07-20), ticket: '722'}
  - {file: src/baldur/settings/runbook.py, symbol: RunbookSettings.async_execution, reason: dead flag - zero production read; the runbook path reads the raw env var and bypasses the field (probe-verified 2026-07-20), ticket: '722'}
  - {file: src/baldur/settings/runbook.py, symbol: RunbookSettings.subscribe_events, reason: dead flag - zero production read; the runbook path reads the raw env var and bypasses the field (probe-verified 2026-07-20), ticket: '722'}
  - {file: src/baldur/settings/settings_recommendation.py, symbol: SettingsRecommendationSettings.fallback_to_rules, reason: dead flag - zero production read (probe-verified 2026-07-20), ticket: '722'}

# G33 (impl doc 576) — the direct-read BALDUR_* registry constant
# (KNOWN_DIRECT_READ_ENV_VARS) must equal the os.environ literal-read set
# discovered across src/baldur. ENFORCED-EMPTY: a drift is fixed by pasting the
# printed add/remove diff into the constant, never baselined.
direct_read_env_registry: []

# G34 (impl doc 578) — no `<module>.logger = structlog.get_logger()` rebind may
# remain anywhere under tests/. The structlog `capture_logs` xdist flake is now
# contained suite-wide by the root conftest `_restore_canonical_structlog_config`
# autouse fixture (578 D2); a re-scattered per-file rebind guard is dead
# duplication. ENFORCED-EMPTY: a module-logger reassignment has no legitimate use
# outside a flake guard — delete it, never baseline it.
structlog_capture_guard: []

# G36 (impl doc 589) — a FEATURE_CATALOG.md entry with Product Status `Parked`
# AND Code Role != `internal-support` must have NO functional importer that lives
# under a SOLD (OSS/PRO product-feature) entry's Module subtree. ENFORCED-EMPTY:
# a surviving SOLD importer is fixed by reclassifying the entry to
# `internal-support` (it IS a live shared module) or removing the coupling, never
# baselined. `internal-support` is the escape hatch (the triage assigns it to
# exactly the entries a sold feature imports).
no_false_dormant: []

# G37 (impl doc 589) — FEATURE_CATALOG.md must not drift from code/settings: no
# obsolete legacy env-var prefix, every cited BALDUR_* resolves to a real settings
# field/direct-read/wildcard prefix, every **Module**: path exists, every product
# entry exposes all three axes. ENFORCED-EMPTY: a drift is fixed (rename the var,
# correct the path, complete the axes), never baselined.
catalog_tier_drift: []
