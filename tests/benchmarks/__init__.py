"""Cat 7A micro-benchmarks (pytest-benchmark + raw quantile capture).

Each test in this package corresponds to a Cat 7A row in
the perf-scenario plan. Per-row evidence files
live under a prior scenario's evidence file (e.g. `7A.1-success-path-overhead.md`).
"""
